# Next.js Firebase API

✅ Ready for **Vercel deployment**.

✅ Endpoints:
- POST `/api/signup`
- POST `/api/business`
- POST `/api/custom`

✅ Uses Firebase Admin SDK with Firestore.

✅ Place your `serviceAccountKey.json` securely or use environment variables in production.
